## 2026.5.31

output 関数がローカル環境では行ごとに flush するように変更した。
multi testcase の問題でテストケースを手入力して試すときに、テストケースごとに結果が表示されないのが不便だったため。

## 2-26.6.14
```cpp
#ifdef LOCAL
using std::endl;
#else
#define endl "\n"
#endif
```
として、ジャッジ環境での無駄なフラッシュを抑えつつ、ローカル環境では出力を逐一行なうという挙動を狙ったが、リアクティブ問題で不慮の事故が起きそうということに気づいたので revert する。
